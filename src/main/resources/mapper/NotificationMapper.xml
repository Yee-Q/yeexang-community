<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="top.yeexang.community.dao.NotificationDao">

    <select id="selectNotReceiveNotificationByReceiverId" parameterType="Long" resultType="Notification">
        SELECT * FROM notification WHERE receiver = #{id} AND status = 0 ORDER BY gmt_create DESC
    </select>

    <insert id="createNotification" parameterType="Notification">
        INSERT INTO notification (notifier,notifier_name,receiver,outerid,outer_title,type,gmt_create,status)
        VALUES (#{notifier},#{notifierName},#{receiver},#{outerid},#{outerTitle},#{type},#{gmtCreate},#{status})
    </insert>

</mapper>